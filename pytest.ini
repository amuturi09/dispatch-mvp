[pytest]
# Repo root on sys.path so tests can `from config import ...`, `from core.engine import ...`
pythonpath = .
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra
